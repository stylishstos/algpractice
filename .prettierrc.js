module.exports = {
    // Maximum line length before wrapping
    printWidth: 80,
    // Use single quotes instead of double quotes
    singleQuote: true,
    // Number of spaces for indentation
    tabWidth: 4,
    // Use tabs instead of spaces (more accessible)
    useTabs: false,
    // Add trailing commas in objects and arrays
    trailingComma: 'es5',
    // Add spaces between brackets in object literals
    bracketSpacing: true,
    // Put the > of a multi-line element at the end of the last line
    bracketSameLine: false,
    // Include parentheses around a sole arrow function parameter
    arrowParens: 'always',
    // Ensure consistent line endings
    endOfLine: 'lf',
    // Format embedded HTML/JSX content
    embeddedLanguageFormatting: 'auto',
    // Put HTML/JSX attributes on new lines
    singleAttributePerLine: true,
    // Enforce semicolons
    semi: true,
    // Indent inside <script> and <style> tags in Vue files
    vueIndentScriptAndStyle: true,
    // Parse markdown text as prose
    proseWrap: 'preserve',
};
